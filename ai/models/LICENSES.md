# Model License Registry

Every model dependency introduced into `ai/` **must** be registered here before it is
wired into an engine, per docs/production-readiness.md risk #3. No weights are
downloaded yet in Milestone 1 — this table records the research decisions from
Milestone 0 (`docs/ai-research.md`, `docs/model-comparison.md`) so they are checked into
version control rather than living only in a conversation.

| Model | Version | Source | License | Commercial use | Checkpoint license verified | Date verified | Notes |
|---|---|---|---|---|---|---|---|
| MediaPipe Face Landmarker (Tasks API, `.task` file) | current | google-ai-edge/mediapipe | Apache 2.0 | ✅ Yes | ✅ | 2026-09-16 | **BLOCKED (Milestone 3):** the Tasks API fetches its `.task` weight file from `storage.googleapis.com` at runtime; this sandbox returns 403 for that host (verified directly, same class of restriction as SAM2/huggingface.co in Milestone 2). Real substitute below (MediaPipe Solutions API, same org, same underlying model family, different packaging) is used instead. |
| MediaPipe Hand Landmarker (Tasks API, `.task` file) | current | google-ai-edge/mediapipe | Apache 2.0 | ✅ Yes | ✅ | 2026-09-16 | **BLOCKED (Milestone 3):** same `storage.googleapis.com` restriction as above. Real substitute below is used instead. |
| MediaPipe Pose Landmarker (Tasks API, `.task` file) | current | google-ai-edge/mediapipe | Apache 2.0 | ✅ Yes | ✅ | 2026-09-16 | **BLOCKED (Milestone 3):** same `storage.googleapis.com` restriction as above. Real substitute below is used instead. |
| MediaPipe Multiclass Selfie Segmenter (Tasks API, `.task` file) | current | google-ai-edge/mediapipe | Apache 2.0 | ✅ Yes | ✅ | 2026-09-16 | **BLOCKED (Milestone 3):** same `storage.googleapis.com` restriction; also would only have provided hair/cloth sub-masks even if reachable, not required for the Milestone 3 MVP. Binary person-mask substitute below is used instead. |
| **MediaPipe Face Detection + Face Mesh** (Solutions/legacy API, `mediapipe==0.10.9`) | 0.10.9 | pypi.org (`files.pythonhosted.org`) / google-ai-edge/mediapipe | Apache 2.0 | ✅ Yes | ✅ | 2026-09-16 | **USED (Milestone 3), real model, not a stand-in for a different algorithm family.** This is the same Google-published face-detection + 468-point face-mesh model family as the Tasks API row above, distributed through the pip package's older "Solutions" API, which bundles its `.tflite` weights **directly inside the wheel** (`mediapipe/modules/face_detection/face_detection_short_range.tflite`, `mediapipe/modules/face_landmark/face_landmark.tflite` — confirmed present via `unzip -l` on the downloaded wheel) and therefore needs **no runtime network fetch at all**. License confirmed via the wheel's own `METADATA` (`License: Apache 2.0`) and the upstream `google-ai-edge/mediapipe` repo LICENSE. `mediapipe>=1.0.0` (the current default `pip install mediapipe` version) **removed** this Solutions API entirely (`dir(mediapipe)` no longer has `.solutions`, confirmed by direct test) and only exposes the network-dependent Tasks API — so this project pins the exact version `mediapipe==0.10.9`, downloaded successfully from `files.pythonhosted.org` (reachable in this sandbox). **Dependency note:** `mediapipe==0.10.9` requires `protobuf<4,>=3.11`, while `rembg`'s `onnxruntime` dependency (Milestone 2) declares `protobuf>=4.25.8`. Both were tested together at `protobuf==3.20.3` in this repo's actual dependency set and both worked for every operation this project performs (`rembg.remove()` produced a valid transparent PNG; all three MediaPipe solutions ran) — see docs/milestone-3-verification.md §2 for the exact commands run. This is a real, verified-working combination for this codebase's specific usage, not a blanket claim that protobuf 3.20.3 supports every onnxruntime feature. |
| **MediaPipe Hands** (Solutions/legacy API, `mediapipe==0.10.9`) | 0.10.9 | pypi.org / google-ai-edge/mediapipe | Apache 2.0 | ✅ Yes | ✅ | 2026-09-16 | **USED (Milestone 3).** Same wheel as above; bundled weights `mediapipe/modules/palm_detection/palm_detection_full.tflite` + `mediapipe/modules/hand_landmark/hand_landmark_full.tflite`, confirmed present, no network fetch. Contrary to this project's earlier (Tasks-API-only) research conclusion that hand landmarks would need to ship as an honest "blocked" component, real hand-landmark inference IS available here — a better outcome than originally assumed, reported as such rather than left as a stale "blocked" note. |
| **MediaPipe Pose** (Solutions/legacy API, `mediapipe==0.10.9`) | 0.10.9 | pypi.org / google-ai-edge/mediapipe | Apache 2.0 | ✅ Yes | ✅ | 2026-09-16 | **USED (Milestone 3).** Same wheel; bundled weights `mediapipe/modules/pose_detection/pose_detection.tflite` + `mediapipe/modules/pose_landmark/pose_landmark_full.tflite`, confirmed present, no network fetch. A segmentation-derived shoulder-line heuristic fallback (`ai/landmarks/pose.py:estimate_from_segmentation`) is also implemented and tested, per the spec's authorized fallback path, but is not the default since the real detector works in this sandbox. |
| **MediaPipe Selfie Segmentation** (Solutions/legacy API, `mediapipe==0.10.9`) | 0.10.9 | pypi.org / google-ai-edge/mediapipe | Apache 2.0 | ✅ Yes | ✅ | 2026-09-16 | **USED (Milestone 3)** for `ai/segmentation/person_segmenter.py` — a real, binary person-vs-background mask. Bundled weights `mediapipe/modules/selfie_segmentation/selfie_segmentation.tflite` + `..._landscape.tflite`, confirmed present, no network fetch. Honest limitation: this is the *binary* selfie segmenter, not the multiclass one (blocked, see row above) — no separate hair/skin/clothing sub-masks are produced; `available_masks` on every `SegmentationResult` is documented as `["person"]` only. |
| OpenCV (`opencv-contrib-python`, Haar cascades) | 5.0.0.93 | opencv/opencv | BSD-3-Clause | ✅ Yes | ✅ | 2026-09-16 | Installed as a transitive dependency of `mediapipe`; not used as the primary face/hand/pose detector in the end (real MediaPipe models were usable directly — see above), kept available as a documented fallback path if a future environment cannot run the bundled MediaPipe wheel. Not currently imported by any Milestone 3 module. |
| SAM2 | 2.1 | facebookresearch/sam2 | Apache 2.0 (code) | ⚠️ Verify checkpoint terms before use | ⚠️ Pending | — | **BLOCKED (Milestone 2):** selected in Milestone 0 for admin-only catalogue background removal, but not integrated — the checkpoint host (huggingface.co) returns a policy-level 403 from this project's build/runtime network egress, verified directly with `curl` (not assumed). Revisit if/when the checkpoint can be self-hosted or mirrored on a reachable host. `rembg`/U-2-Net (below) is the real, working substitute used for Milestone 2. |
| rembg | 2.0.59 | github.com/danielgatis/rembg | MIT | ✅ Yes | ✅ | 2026-09-16 | Fetched `LICENSE.txt` directly from the repo to confirm — MIT, no restrictions. Thin Python wrapper around ONNX inference; the actual model is U-2-Net (next row). Used for catalogue asset background removal (`ai/catalogue/background_remover.py`), admin-only path, CPU-only via onnxruntime. |
| U-2-Net (u2net.onnx) | v0.0.0 release asset | github.com/xuebinqin/U-2-Net (weights re-hosted by rembg's GitHub release) | Apache 2.0 | ✅ Yes | ✅ | 2026-09-16 | Fetched `LICENSE` directly from the model's own repo to confirm — Apache 2.0. Salient object segmentation, not jewellery-specific, but effective for "product on plain/simple background" catalogue photos, which is the realistic admin upload scenario. Not licensed or intended for the user-facing try-on path (out of scope for Milestone 2 regardless). |
| IDM-VTON | — | yisol/IDM-VTON | CC BY-NC-SA 4.0 | ❌ No | ✅ | 2026-09-16 | Reference/research only — excluded from this product |
| OOTDiffusion | — | levihsu/OOTDiffusion | CC BY-NC-SA 4.0 | ❌ No | ✅ | 2026-09-16 | Reference/research only — excluded |
| CatVTON | — | Zheng-Chong/CatVTON | CC BY-NC-SA 4.0 | ❌ No | ✅ | 2026-09-16 | Reference/research only — excluded |
| StableVITON | — | rlawjdghek/StableVITON | CC BY-NC-SA 4.0 | ❌ No | ✅ | 2026-09-16 | Reference/research only — excluded |

**Rule:** no PR may add a model import under `ai/` or `ai/models/` without a corresponding
row in this table, including the "date verified" and a link/citation for the license
claim. A row with an unresolved "⚠️ Verify" status blocks that model from being used in
a production code path (admin-only offline tooling is the one exception, and only if the
risk is explicitly called out in the PR description).
